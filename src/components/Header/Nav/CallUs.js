import { ModalButton, ButtonDiv } from "../styles";

const CallUs = () => {
  return (
      <ButtonDiv>
          <ModalButton>Call us</ModalButton>
      </ButtonDiv>
  );
};

export default CallUs;